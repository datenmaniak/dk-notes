<x-guest-layout>
    <div class="min-h-screen flex flex-col sm:justify-center items-center pt-6 sm:pt-0 bg-(--bg-primary) transition-colors duration-300">
        <div class="w-full sm:max-w-md mt-6 px-6 py-4 card shadow-xl overflow-hidden">
            
            {{-- Logo o ícono con tu color de marca brand.glow --}}
            <div class="flex justify-center mb-6">
                <div class="w-16 h-16 bg-brand-glow rounded-full flex items-center justify-center shadow-lg shadow-brand-glow/20">
                    <span class="text-3xl text-white">📝</span>
                </div>
            </div>
            
            {{-- Título y subtítulo dinámicos --}}
            <h1 class="text-2xl font-bold text-center text-(--text-primary) mb-2">Bienvenido</h1>
            <p class="text-center text-(--text-secondary) text-sm mb-6">Gestiona tus notas de forma eficiente</p>
            
            {{-- Mensajes de error --}}
            @if ($errors->any())
                <div class="mb-4 p-3 bg-red-500/10 border border-red-500/30 rounded-lg">
                    @foreach ($errors->all() as $error)
                        <p class="text-sm text-red-500 font-medium">{{ $error }}</p>
                    @endforeach
                </div>
            @endif
            
            {{-- Mensaje de éxito --}}
            @if (session('status'))
                <div class="mb-4 p-3 bg-green-500/10 border border-green-500/30 rounded-lg">
                    <p class="text-sm text-green-500 font-medium">{{ session('status') }}</p>
                </div>
            @endif
            
            {{-- Formulario --}}
            <form method="POST" action="{{ route('login') }}">
                @csrf
                
                {{-- Email --}}
                <div class="mb-4">
                    <label for="email" class="block text-sm font-medium text-(--text-primary) mb-1">
                        Correo electrónico
                    </label>
                    <input id="email" type="email" name="email" value="{{ old('email') }}" required autofocus
                           class="w-full px-3 py-2 input-base rounded-lg text-(--text-primary)">
                </div>
                
                {{-- Contraseña --}}
                <div class="mb-4">
                    <label for="password" class="block text-sm font-medium text-(--text-primary) mb-1">
                        Contraseña
                    </label>
                    <input id="password" type="password" name="password" required
                           class="w-full px-3 py-2 input-base rounded-lg text-(--text-primary)">
                </div>
                
                {{-- Recordarme --}}
                <div class="flex items-center justify-between mb-4">
                    <label class="flex items-center cursor-pointer">
                        <input type="checkbox" name="remember" class="rounded border-(--border-color) bg-(--bg-input) text-brand-glow focus:ring-brand-glow">
                        <span class="ml-2 text-sm text-(--text-secondary)">Recordarme</span>
                    </label>
                    
                    @if (Route::has('password.request'))
                        <a href="{{ route('password.request') }}" class="text-sm text-brand-glow hover:text-brand-accent transition-colors">
                            ¿Olvidaste tu contraseña?
                        </a>
                    @endif
                </div>
                
                {{-- Botón Principal usando tu paleta brand --}}
                <button type="submit" class="w-full bg-brand-glow hover:bg-brand-accent text-white font-semibold py-2 px-4 rounded-lg shadow-md hover:shadow-brand-glow/20 transition duration-200 cursor-pointer">
                    Iniciar sesión
                </button>
            </form>
            
            {{-- Enlace a registro --}}
            <div class="text-center mt-6">
                <p class="text-sm text-(--text-secondary)">
                    ¿No tienes cuenta?
                    <a href="{{ route('register') }}" class="text-brand-glow hover:text-brand-accent font-medium transition-colors">
                        Regístrate aquí
                    </a>
                </p>
            </div>
        </div>
    </div>
</x-guest-layout>